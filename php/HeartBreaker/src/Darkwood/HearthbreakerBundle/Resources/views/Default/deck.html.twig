{% extends "HearthbreakerBundle::layout.html.twig" %}

{% block content %}
	<div class="panel panel-default">
		<div class="panel-body">
			<table class="table table-striped">
				<thead>
					<tr>
						<th>Title</th>
						<th>Cost</th>
						<th>Note</th>
					</tr>
				</thead>
				<tbody>
				{% for deck in decks %}
					<tr>
						<td><a href="{{ path('deck_detail', {'slug': deck.slug}) }}">{{ deck.name }}</a></td>
						<td></td>
						<td>{{ deck.voteUp }} - {{ deck.voteDown }}</td>
					</tr>
				{% endfor %}
				</tbody>
			</table>
		</div>
	</div>
{% endblock %}
