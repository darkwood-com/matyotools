imports:
    - { resource: parameters.yml }
    - { resource: security.yml }

framework:
    #esi:             ~
    #translator:      { fallback: "%locale%" }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver, add the path in parameters.yml
        # e.g. database_path: "%kernel.root_dir%/data/data.db3"
        # path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }

matyotools_harvest_app:
  user: %harvest_user%
  password: %harvest_password%
  account: %harvest_account%
  domino_projects:
    7376843: { name: AO NRJ GAMES                              , client: '', dossier: '' }
    6664888: { name: EVOLUPHARM                                , client: '', dossier: '' }
    7557823: { name: Heineken - Beertender (heineken)          , client: '', dossier: 140618 }
    6445332: { name: KRONENBOURG - Tourtel Twist               , client: '', dossier: '' }
    6579884: { name: GRATTA E VINCI - Lottomatica              , client: '', dossier: '' }
    6938786: { name: PRIMONIAL - Partenaires                   , client: '', dossier: 140607 }
    7074717: { name: PRIMONIAL - Vie Courante 2015 (Primonial) , client: '', dossier: 140583 }
    4802473: { name: Refonte Site Master Decléor (Decléor)     , client: '', dossier: '' }
    7557808: { name: Salomon - Service plateform (Salomon)     , client: '', dossier: '' }
    7881273: { name: Site expérience déco (ACOVA)              , client: '', dossier: 140517 }
    2325033: { name: SoMusic (Universal)                       , client: '', dossier: '' }
    8347267: { name: Sweet Kiss in NY (Lolita Lempicka)        , client: '', dossier: '' }
    6329675: { name: TALENTSOFT GLOBAL (Talentsoft)            , client: '', dossier: 140490 }

matyotools_domino:
  user: %domino_user%
  password: %domino_password%
